/* Simple reset */
*{box-sizing:border-box}body,html{margin:0;font-family:Inter,system-ui,Arial,sans-serif;color:#222}
a{color:inherit;text-decoration:none}

/* Container */
.container{max-width:1100px;margin:0 auto;padding:0 18px}

/* Top strip */
.top-strip{background:#f1f5f9;color:#0f172a;font-size:14px}
.top-strip .container{display:flex;justify-content:space-between;padding:8px 18px}
.top-strip a{color:#0f172a;font-weight:600}

/* Header */
.site-header{background:#ffffff;border-bottom:1px solid #e6edf3}
.header-inner{display:flex;align-items:center;justify-content:space-between;padding:14px 0}
.brand{display:flex;align-items:center;gap:12px}
.logo{width:56px;height:56px;object-fit:cover;border-radius:8px}
.site-header h1{margin:0;font-size:20px}
.site-header small{display:block;color:#6b7280;font-size:12px}

/* Nav */
.main-nav ul{list-style:none;display:flex;align-items:center;gap:16px;margin:0;padding:0}
.main-nav a{padding:8px 6px;display:inline-block}
.btn{display:inline-block;padding:8px 12px;border-radius:6px;background:#f3f4f6;border:1px solid #e6edf3}
.btn-primary{background:#ff6600;color:#fff;border:none}
.btn-cta{background:#0077cc;color:#fff;border:none;padding:10px 16px;border-radius:6px}

/* Dropdown */
.dropdown{position:relative}
.dropdown-menu{position:absolute;left:0;top:100%;background:#fff;border:1px solid #e6edf3;min-width:180px;display:none;padding:8px 0;z-index:50}
.dropdown-menu li{padding:0}
.dropdown-menu a{display:block;padding:8px 12px}
.dropdown:hover .dropdown-menu{display:block}

/* Hamburger (mobile) */
.menu-toggle{display:none;background:none;border:none;font-size:22px;cursor:pointer}

/* Hero */
.hero{position:relative;overflow:hidden}
.hero-slider{display:flex;transition:transform 0.5s ease}
.slide{min-height:340px;background-size:cover;background-position:center;flex:0 0 100%;display:flex;align-items:center}
.hero-content{color:#fff;padding:40px 18px;background:linear-gradient(90deg,rgba(0,0,0,0.45),rgba(0,0,0,0.1));border-radius:6px}
.slider-controls{display:flex;justify-content:flex-end;gap:8px;padding:8px 18px}

/* Cards */
.services .cards{display:flex;gap:18px;margin-top:18px}
.card{background:#fff;border-radius:8px;box-shadow:0 6px 18px rgba(15,23,42,0.06);padding:14px;flex:1;text-align:center}
.card img{width:100%;height:140px;object-fit:cover;border-radius:6px;margin-bottom:10px}

/* Packages small cards */
.packages .cards.small{display:flex;gap:12px}
.cards.small .card{padding:12px}

/* Testimonials */
.testimonials blockquote{background:#fff;padding:18px;border-radius:8px;box-shadow:0 6px 18px rgba(15,23,42,0.06);margin:12px 0}

/* CTA */
.cta .box{background:#004080;color:#fff;padding:18px;border-radius:8px;text-align:center}

/* Footer */
.site-footer{background:#071A2E;color:#fff;padding:30px 0;margin-top:30px}
.footer-grid{display:grid;grid-template-columns:1fr 1fr 1fr;gap:18px}
.copyright{text-align:center;padding:12px 0;color:#9fb0c8;font-size:13px}

/* Modal */
.modal{position:fixed;left:0;top:0;width:100%;height:100%;display:none;align-items:center;justify-content:center;background:rgba(2,6,23,0.6);z-index:200}
.modal-panel{background:#fff;padding:18px;border-radius:8px;max-width:520px;width:92%;position:relative}
.modal-close{position:absolute;right:10px;top:10px;border:none;background:none;font-size:18px;cursor:pointer}

/* Forms */
input,select,textarea{width:100%;padding:10px;margin:6px 0;border:1px solid #e6edf3;border-radius:6px}

/* Responsive */
@media (max-width:900px){
  .header-inner{gap:12px}
  .main-nav ul{display:none;position:absolute;right:0;top:64px;background:#fff;padding:12px;border-radius:6px;box-shadow:0 6px 18px rgba(2,6,23,0.08)}
  .main-nav ul.show{display:flex;flex-direction:column;min-width:220px}
  .menu-toggle{display:block}
  .services .cards{flex-direction:column}
  .packages .cards.small{flex-direction:column}
  .footer-grid{grid-template-columns:1fr}
  .hero .slide{min-height:260px}
  .dropdown:hover .dropdown-menu{display:none} /* disable hover dropdown on mobile */
}
